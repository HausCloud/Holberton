#include "holberton.h"

/**
 * cap_string - capitalize first letter of word depending on separators
 * Return: something
 */

char *cap_string(char *)
{

	while (str != NULL)
	{
		putchar(*str);
		str++;
	}

	return (0);
}
